package ClassAndObject;

public class Calculator {
	static int powerInt(int num1,int num2)
	{
		int p = (int)Math.pow(num1, num2);
		return p;
	}
	
	static double powerDouble(double num1,double m2)
	{
		double p1 = Math.pow(num1, m2);
		return p1;
	}
}
